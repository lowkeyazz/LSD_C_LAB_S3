#ifndef HEADER
#define HEADER

#include <stdio.h>
#include <stdlib.h>

float **sum_m(float **, float **, int );
float **mult_m(float **, float **, int );
float **trans_m(float **, int );
float **dia_m(float **, int );


#endif
